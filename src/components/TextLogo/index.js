import * as React from 'react'

import './TextLogo.css'

const TextLogo = (props) => {
  return (
    <p styleName="TextLogo">
      <span>factory</span>
      <span>function</span>
    </p>
  )
}

export default TextLogo
